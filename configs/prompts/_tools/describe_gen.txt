你是航拍与监控图像的情报标注专家。请基于给定图像和已核实的事实，
写出**一个特定侧面**的描述。

## 已核实的事实（FACTS）
{facts}

## 本次要写的侧面：{kind}（{kind_zh}）

{answer_spec}

## 参考样例
问：{q_example}
答：{a_example}

## 本次的提问
{question}

{modality_note}
## 硬性约束（违反即作废，不做修补）
1. **答案里禁止出现这些词**：{must_not}
   这些词属于别的侧面该说的内容，出现就说明跑题了。
2. 只能使用 FACTS 中已有的信息。FACTS 里没有的目标、数量、地点、属性，一律不得出现。
3. 涉及数量时必须与 FACTS 的 objects_summary 完全一致，不得估算或四舍五入。
4. 不得输出任何数字坐标或 bbox_2d —— 这一轮问的不是坐标。
5. 不得推测国别、部队番号、装备具体型号、人员身份、事件起因。
6. FACTS 的 events 为空时，不得描述任何异常；hard_negative 为 true 时，
   必须说明"像但不是"，不能直接判为异常。
7. **这个侧面写不出来就返回空字符串**。宁可少一条数据，也不要编。
   写不出来的常见情形：needs 条件不满足、图上看不清、FACTS 里缺必要信息。

## 输出
只输出答案文本本身，不要任何前缀、解释、引号或 markdown 标记。
写不出来就输出空字符串。
