# 越界目标计数。关键在于同时问"越了的"和"没越的"——
# 只问越界数, 模型容易学成"画面里有几个动的就答几个"。
#! task: cross_count
#! needs: crossing
#! forbid: 坐标 框出 边界框
#! max-len: 36
有几个目标越过了界线？
统计一下越界目标的数量。
画面中有多少目标穿过了边界？
越界的和没越界的各有多少？
清点越过界线的目标数。
有多少目标始终停在界线同一侧？
越界目标共几个？
数一下穿越边界的目标。
