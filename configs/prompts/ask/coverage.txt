# 烟火这类不可数目标的问法: 问覆盖范围, 不问"几个"。
# "清点一下画面中的烟雾"是不成立的问法 —— 标注里的一个框标的是范围, 不是"一个烟雾"。
#! task: coverage
#! needs: uncountable
#! forbid: 几个 多少个 数量 清点 统计 计数
#! max-len: 32
{zh}覆盖了画面多大范围？
这片{zh}大致占画面的多少？
{zh}的波及范围有多大？
描述一下{zh}的覆盖情况。
{zh}在画面中扩散到了什么程度？
这处{zh}影响的范围如何？
{zh}占据了画面的哪一块、有多大？
说明{zh}的分布范围。
