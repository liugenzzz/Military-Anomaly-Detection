# 方位指代的问法池。【绝不能出现要坐标的动词】——
# 实测问「什么位置」时模型给的是「左上角」这类方位描述，不是 bbox。
# 问法和答案形态必须一致，否则会逼模型学一个反常识的映射。
#! task: locate_verbal
#! forbid: 框出 坐标 边界框 检测框 标出 圈出 定位 bbox
#! max-len: 36
{zh}出现在画面的什么位置？
{zh}处在画面的哪一块？
这处异常在画面的什么方位？
{zh}靠近画面的哪条边？
异常区域大概占据画面的哪个部分？
从整幅画面看，这处异常在什么方位上？
这片区域相对画面中心偏向哪边？
说明异常所处的大致方位。
{zh}大致在画面的哪个区域？
它在画面里的位置怎么描述？
这处异常落在画面的什么方位？
异常大致处于画面的哪个方向？
描述一下异常所在的方位。
