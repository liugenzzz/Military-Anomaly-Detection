# 困难负样本专用: 问"目标最密集的区域在哪", 而不是"异常在哪"。
# 这类图有真实的密集区(民用停车场、民航机坪), 区域信息不该浪费; 但画面被判为
# 正常, 再问"异常在哪"就和判定结论打架了。
#! task: dense_region
#! needs: hard_negative
#! forbid: 异常 越界 集结 爆炸 烟雾
#! max-len: 34
画面中目标最密集的区域在什么位置？
目标聚集得最集中的是哪一块？
哪个区域的目标分布最密？
说明目标扎堆的大致方位。
密度最高的那片区域在画面哪里？
目标最集中的地方在什么方位？
指出画面中目标最密的一片。
这些目标主要集中在哪一带？
