# 计数并框出的问法池。答案同时给数量和每个目标的坐标框。
# 这类问法必须【明确要求框】, 否则模型只会回一句数量。
#! task: count_box
#! forbid: 方位 左上 右下 大致位置
#! max-len: 80
画面中有多少{mw}{label}？把它们逐个框出来。
框出画面里的全部{label}，并给出数量。
请以边界框标出所有{label}，同时说明共有几{mw}。
把{label}全部标注出来，附上总数。
逐个框选画面中的{label}并计数。
画面里的{label}有几{mw}？请给出每一个的边界框。
标出所有{label}的位置并报出总数。
画面中有多少{mw}{label}？把它们逐个框出来。先给数量，再以 JSON 数组输出每个目标的 bbox_2d 与 label。
框出画面里的全部{label}，并给出数量。先给数量，再以 JSON 数组输出每个目标的 bbox_2d 与 label。
请以边界框标出所有{label}，同时说明共有几{mw}。先给数量，再以 JSON 数组输出每个目标的 bbox_2d 与 label。
